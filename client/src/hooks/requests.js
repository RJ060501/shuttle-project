async function httpGetBookings() {
  // TODO: Once API is ready.
  // Load launches, sort by flight number, and return as JSON.
}

async function httpSubmitBooking(launch) {
  // TODO: Once API is ready.
  // Submit given launch data to launch system.
}

async function httpAbortBooking(id) {
  // TODO: Once API is ready.
  // Delete launch with given ID.
}

export {
  httpGetBookings,
  httpSubmitBooking,
  httpAbortBooking,
};